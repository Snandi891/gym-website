/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://gym-website-six-livid.vercel.app", // 🔴 Replace with your domain (or vercel URL)
  generateRobotsTxt: true, // also generates robots.txt
  sitemapSize: 7000, // splits sitemap if too many URLs
};
